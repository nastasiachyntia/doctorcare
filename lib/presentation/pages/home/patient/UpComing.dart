import 'package:flutter/material.dart';

class UpComing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(' This Feature is still in development'),
    );
  }
}
